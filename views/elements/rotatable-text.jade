- var xEm = pageElement.getProperty('Offset X').value / sizeAdjust, yEm = pageElement.getProperty('Offset Y').value / sizeAdjust, deg = pageElement.getProperty('Rotate').value;
span.text.element.placeable.rotatable(data-id=cmsItemID, style="left:#{xEm}em;top:#{yEm}em;transform:rotate(#{deg}deg);-ms-transform:rotate(#{deg}deg);-moz-transform:rotate(#{deg}deg);-webkit-transform:rotate(#{deg}deg)") !{parsedText}